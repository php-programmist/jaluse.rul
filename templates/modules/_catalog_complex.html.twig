<div class="products">
    {% for item in items %}
        <div class="product">
            <a href="/{{item.uri}}/" target="_blank">
                <div class="product__image_wrapper">
                    <img {{ lazy_load(asset(item.cardImageUrl) | imagine_filter('catalog_thumb')) }} alt="{{ item.name }}">
                </div>
            </a>
            <div class="product__name">
                <a href="/{{item.uri}}/" target="_blank">{{ item.name }}</a>
            </div>
            <div class="product__price">
                    {{ min_price(item) }}
            </div>
            <div class="d-flex justify-content-center">
                <button class="mbtn mbtn_orange modal-open" data-title="Рассчитать">Рассчитать</button>
            </div>
        </div>
    {% endfor %}
</div>