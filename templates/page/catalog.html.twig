{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('catalog') }}
    {{ encore_entry_link_tags('simple_catalog') }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('catalog') }}
    {{ encore_entry_script_tags('simple_catalog') }}
{% endblock %}

{% block title %}
    {%- if page.title -%}
        {{ page.title }}
    {%- else -%}
        {{ page.name }} купить в Москве. Каталог и цены | Мастерская Жалюзи
    {%- endif -%}
{% endblock %}
{% block description %}
    {%- if page.description -%}
        {{ page.description }}
    {%- else -%}
        {{ page.name }} купить недорого в Москве. ⭐ Выезд и замер бесплатно! ✅ Изготовление жалюзи с установкой за 1-4 дня. ✅ Гарантия 2 года. ⭐ {{ page.name }} по низким ценам в интернет магазине «Мастерская жалюзи» ☎ 8-800-775-72-38.
    {%- endif -%}
{% endblock %}
{% block h1 %}{{ page.name }} в Москве{% endblock %}

{% block body %}
    {% block location_block %}{% endblock %}
    <div class="container-fluid catalog-container">
        {{ include('modules/rating_schema.html.twig') }}
        <h2>{% block content_h2 %}{{ page.name }}{% endblock %}</h2>
        <div id="app">
            <v-category-page-calc
                    type_id="{{ page.type.id??0 }}"
                    material_id="{{ page.material.id??0 }}"
                    force_show_filters="{{ force_show_filters ?? 0 }}"
            >
            </v-category-page-calc>
        </div>
    </div>
    {% if items is not empty %}
        <div class="container-fluid catalog-container">
            <div class="row catalog-calculator">
                <div class="offset-md-2 col-md-9 col-sm-12">
                    <h2>Рекомендуемые {{ page.name }}:</h2>
                    {{ include('modules/_catalog_complex.html.twig',{needWrapper: false}) }}
                </div>
            </div>
        </div>
    {% endif %}
    {% block catalog_card %}
        {{ include('modules/_catalog_card.html.twig') }}
    {% endblock %}

    {% block rating_block %}
        {{ include('modules/rating.html.twig') }}
    {% endblock %}

    {{ include('modules/_seo_block.html.twig') }}
    {% if show_calc %}
        {{ include('modules/_calculator.html.twig') }}
    {% endif %}

    {{ include('modules/_our_works.html.twig') }}
    {{ include('modules/_employees.html.twig') }}
    {{ include('modules/_clients.html.twig') }}
{% endblock %}
